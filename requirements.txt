# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy==2.0.35
psycopg2-binary>=2.9.9
alembic==1.13.3

# PDF processing — pdfplumber (primary) + PyMuPDF (fallback)
pdfplumber==0.11.4
PyMuPDF==1.24.9

# HTTP client — used for Gemini API calls (no extra SDK needed, httpx is enough)
httpx==0.27.2
google-generativeai>=0.8.0

# Data validation
pydantic==2.9.2

# File handling
aiofiles==24.1.0

# Environment
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Code quality
ruff==0.6.9

# NOTE: anthropic package removed — switched to Gemini Flash free tier
# Gemini API is called directly via httpx (no SDK needed)
# Get free API key: aistudio.google.com
